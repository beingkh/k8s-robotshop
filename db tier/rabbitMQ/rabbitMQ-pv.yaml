apiVersion: v1
kind: PersistentVolume
metadata:
  name: rabbitmq-pv # Descriptive name
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem # Correct (RabbitMQ needs a folder)
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/rabbitmq-data" # Separate folder from MySQL!
