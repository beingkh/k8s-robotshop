apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv # Descriptive name
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem # Correct (RabbitMQ needs a folder)
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/redis-data" # Separate folder from MySQL!
